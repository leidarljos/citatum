# Marks

Ink `#0B0B0A`. Cream `#F4F0E6`. Gold `#E6A23C`. Rounded square `rx=36` on a `200` viewBox.

The gold pin is the only shared atom. Do not invent a fifth pictorial family. Wordmarks are SVG, not a generated raster.
